/**
 * Figma EZER-KEY node 1707:7213 "Trusted Brands" section. The design itself
 * has no brand logos placed here (just an empty strip) — rendered as-is
 * rather than inventing logos; add real vendor/brand marks when available.
 */
export function TrustedBrands() {
  return (
    <section className="bg-surface px-10 py-12 text-center">
      <h2 className="text-2xl font-bold text-[#0f3e17]">Trusted Brands</h2>
      <div className="mx-auto mt-8 h-40 max-w-5xl rounded-[10px] bg-surface-muted" aria-hidden />
    </section>
  );
}
