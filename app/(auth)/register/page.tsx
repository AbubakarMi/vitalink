/** Placeholder — UI comes later. See lib/api/auth.ts's register() for the wired call. */
export default function RegisterPage() {
  return (
    <main>
      <h1>Create an account</h1>
      <p>Form pending — see lib/api/auth.ts.</p>
    </main>
  );
}
